#include <iostream>

#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>

int main() {
  std::cout << "hello world" << std::endl;

  // int width, height, component_num;
  // std::string path = "data/diffuse.jpg";
  // unsigned char *data =
  //     stbi_load(path.c_str(), &width, &height, &component_num, 0);
  // std::cout << sizeof(*data) << std::endl;

  return 0;
}